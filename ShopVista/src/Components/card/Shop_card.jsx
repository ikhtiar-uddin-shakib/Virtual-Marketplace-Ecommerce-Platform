
// eslint-disable-next-line react/prop-types
const Shop_card = ({shop_card}) => {
    // eslint-disable-next-line react/prop-types
    const {id} = shop_card;
    return (
        <div>
            <h2>{id}</h2>
            
        </div>
    );
};

export default Shop_card;